<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:label="Salvox Charge Overlay"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">

        <!--
          Pantalla desde la que se concede el permiso "Mostrar sobre otras apps",
          imprescindible para que el overlay pueda dibujarse en segundo plano.
        -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Servicio que dibuja la animacion como ventana superpuesta (overlay).
          A diferencia de una Activity, esto SI puede lanzarse desde un
          BroadcastReceiver en segundo plano sin chocar con las restricciones
          de Android 10+ / MIUI sobre "iniciar actividades en background".
        -->
        <service
            android:name=".ChargeOverlayService"
            android:exported="false" />

        <!--
          ACTION_POWER_CONNECTED esta en la lista de broadcasts implicitos
          exentos de las restricciones de Android 8+, asi que este receptor
          declarado en el manifest funciona sin necesitar un servicio en
          primer plano corriendo todo el rato.
        -->
        <receiver
            android:name=".PowerConnectReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
